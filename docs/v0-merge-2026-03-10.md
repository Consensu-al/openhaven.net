# v0 Visual Merge — 2026-03-10

## Source
- Fork repo: `zachmiltz/oh-v0`
- Remote name: `v0-fork`
- 44 commits merged (fast-forward from `8fdf97d` to `a48e374`)

## What v0 Changed (Visual Redesign)

### Components Modified
- **AffordancesPanel.tsx** — Replaced shadcn ToggleGroup with plain styled buttons. New StepHeader component for step numbering. Simplified layout with left-aligned indent.
- **DomainCard.tsx** — Redesigned with rounded-2xl cards, colored square icon container, check mark badge on selection. Removed `cn()` utility usage.
- **DomainGrid.tsx** — Simplified from trust-trajectory clustering (Connecting/Deepening/Acting/More) to a flat grid. Intentional simplification.
- **GuidedDiscoveryCTA.tsx** — Added `variant` prop (`'full-width' | 'centered'`). New icon (Compass/Sparkles). Button text changed to "Guide me".
- **NavigatorSection.tsx** — Replaced hand-rolled step headings with StepHeader component. Added Radix Tooltip on Step 3 results heading. GuidedDiscoveryCTA removed from visible layout (wizard dialog still functional).
- **ProtocolCard.tsx** — Entire card redesigned. Footer bar with arrow replaces inline "View Details" link. Date format simplified to "Month Year".
- **DisclaimerBanner.astro** — Complete visual redesign from full-width banner to centered pill/card with extracted "Alpha" badge.
- **PrinciplesSection.astro** — Redesigned from card grid to horizontal circle-step flow. Heading changed to "How OpenHaven Works". Added `id` prop.

### Pages Modified
- **index.astro** — Principle 4 renamed "Connect & Build". Hero badge updated to alpha status. Primary CTA now links to `#how-it-works`.
- **domains/[slug].astro** — Complete page redesign. Hero card layout with stats. Content grid for affordances + overview. Protocol cards now hydrated React components.
- **protocols/[id].astro** — Complete page redesign. Hero card with entity badge, badge row, links section. Two-column content grid for attributes and tags.

### New Files
- `src/components/islands/StepHeader.tsx` — Shared numbered step heading component
- `src/components/islands/AffordancesVariantsShowcase.tsx` — Design variant demo (non-production)
- `src/components/islands/NavigatorVariantsShowcase.tsx` — Design variant demo (non-production)
- `src/pages/affordances-variants.astro` — Demo page (not linked in nav)
- `src/pages/navigator-variants.astro` — Demo page (not linked in nav)
- `components.json` — shadcn/ui configuration

### Config Changes
- `astro.config.mjs` — Build format changed from `'file'` to `'directory'` (clean URLs)

## Post-Merge Fixes Applied

### Blockers Fixed
1. **JSON-LD restored** on both `domains/[slug].astro` (CollectionPage + BreadcrumbList schemas) and `protocols/[id].astro` (TechArticle schema). v0 had removed all structured data.
2. **ARIA accessibility restored** on `AffordancesPanel.tsx` — added `role="radiogroup"`, `aria-checked` on match mode buttons; `role="group"`, `aria-label`, `aria-pressed` on affordance chips.
3. **Hardcoded hex colors replaced** in `protocols/[id].astro` — `RISK_COLOR` now uses `var(--color-risk-low/medium/high)` design tokens instead of raw `#22c55e`, `#f59e0b`, `#ef4444`.
4. **Nested interactives fixed** in `ProtocolCard.tsx` — root element changed from `<a>` to `<div>`. The "View Details" footer is now the sole `<a>` link, avoiding invalid button-inside-anchor nesting with the tooltip trigger.

### Breadcrumbs Restored
- Added proper breadcrumb navigation to both `domains/[slug].astro` and `protocols/[id].astro` with accessible markup (`aria-label`, `aria-current="page"`, 44px touch targets).

### Container Max-Width Standardized
- Both detail pages now use `max-width: 1200px` (matching the global `.container` definition) instead of the inconsistent 56rem/64rem values v0 introduced.

### Lock File / Dependencies
- Removed `pnpm-lock.yaml` and v0 helper scripts (`scripts/generate-lock.mjs`, `scripts/install-deps.sh`)
- Restored `bun.lock` as the canonical lock file
- Restored Zod to `^4.3.6` (v0 had downgraded to `^3.25.0`)

## Intentional Changes Accepted (Not Reverted)
- GuidedDiscoveryCTA removed from navigator section view
- Domain trust-trajectory clustering replaced with flat grid
- Hero CTA anchor changed from `#navigator` to `#how-it-works`
- Community link removed from ProtocolCard (available on detail page)
- Build format changed to `'directory'` for clean URLs
